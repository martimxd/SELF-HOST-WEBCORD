# Contributing

Create one branch per change, keep TypeScript strict, and include tests for new behavior.

```bash
npm install
npm run lint
npm test
npm run build
```

Do not add secrets, user-uploaded files, or visual elements from proprietary products. Pull requests should explain the impact, migrations, and how the change was tested.
